<Controls:MetroWindow x:Class="Lama.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lama"
        xmlns:controls="clr-namespace:Lama.UI.UC"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="{Binding SystemParameters.PrimaryScreenHeight}" 
        Width="{Binding SystemParameters.PrimaryScreenWidth}"
        ShowTitleBar="False" ShowDialogsOverTitleBar="True" ShowMaxRestoreButton="False"
        ResizeMode="CanMinimize"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="2">

    <Grid Margin="30">
        <Controls:MetroAnimatedTabControl Margin="10" Grid.Row="1" Grid.Column="1">
            <Controls:MetroTabItem Header="Tournoi">
                
            </Controls:MetroTabItem>
            <Controls:MetroTabItem Header="Locaux">
                <controls:Locaux />
                
            </Controls:MetroTabItem>
        </Controls:MetroAnimatedTabControl>
        
        <TextBlock Grid.Row="0" HorizontalAlignment="Right" Margin="0,10,20,0">
            <Hyperlink>S'authentifier</Hyperlink>
        </TextBlock>
    </Grid>
</Controls:MetroWindow>
